using System.Windows.Controls;

namespace StudComp.Views.Archivist.Dialogs;

/// <summary>Диалог выбора режима импорта правил. Логики нет — всё во ViewModel.</summary>
public partial class RulesImport : UserControl
{
    public RulesImport() => InitializeComponent();
}
