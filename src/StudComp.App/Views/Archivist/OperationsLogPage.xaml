<UserControl x:Class="StudComp.Views.Archivist.OperationsLogPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:conv="clr-namespace:StudComp.Resources.Converters"
             xmlns:vm="clr-namespace:StudComp.ViewModels.Archivist"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:OperationsLogViewModel}"
             d:DesignHeight="600" d:DesignWidth="900">

    <Grid Margin="0,12,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" VerticalAlignment="Center" TextWrapping="Wrap"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="12"
                       Text="Последние перемещения файлов архивариусом. «Отменить» вернёт файл на прежнее место, если он ещё там и путь свободен. Наведите на запись, чтобы увидеть кнопку «Отменить»." />

            <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="12,0,0,0">
                <ui:Button Command="{Binding ReconcileCommand}" Appearance="Secondary" Margin="0,0,8,0"
                           Icon="{ui:SymbolIcon FolderSync24}" Content="Проверить папки"
                           ToolTip="Сверить содержимое наблюдаемых папок с учётом — на случай, если наблюдатель пропустил файлы" />
                <ui:Button Command="{Binding RefreshCommand}" Appearance="Secondary"
                           Icon="{ui:SymbolIcon ArrowSync24}" Content="Обновить" />
            </StackPanel>
        </Grid>

        <!-- Фильтры -->
        <Grid Grid.Row="1" Margin="0,0,0,12"
              Visibility="{Binding IsEmpty, Converter={x:Static conv:InverseBooleanToVisibilityConverter.Instance}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ui:SymbolIcon Grid.Column="0" Symbol="Filter24" VerticalAlignment="Center" Margin="0,0,8,0"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            <ComboBox Grid.Column="1" Margin="0,0,8,0" MinWidth="140"
                      ItemsSource="{Binding Source={x:Static vm:ArchivistChoices.DateFilters}}"
                      DisplayMemberPath="Display" SelectedValuePath="Value"
                      SelectedValue="{Binding DateFilter, Mode=TwoWay}" />
            <ComboBox Grid.Column="2" Margin="0,0,8,0" MinWidth="140"
                      ItemsSource="{Binding Source={x:Static vm:ArchivistChoices.OutcomeFilters}}"
                      DisplayMemberPath="Display" SelectedValuePath="Value"
                      SelectedValue="{Binding OutcomeFilter, Mode=TwoWay}" />
            <ComboBox Grid.Column="3" MinWidth="160"
                      ItemsSource="{Binding Subjects}" DisplayMemberPath="Name"
                      SelectedItem="{Binding SubjectFilter, Mode=TwoWay}" />

            <ui:Button Grid.Column="4" HorizontalAlignment="Right" Appearance="Transparent"
                       Content="Сбросить фильтры" Command="{Binding ResetFiltersCommand}"
                       Visibility="{Binding HasActiveFilter, Converter={x:Static conv:BooleanToVisibilityConverter.Instance}}" />
        </Grid>

        <ui:InfoBar Grid.Row="2" VerticalAlignment="Top" Severity="Informational" IsClosable="False"
                    Title="Журнал пуст"
                    Message="Как только архивариус разложит первый файл, операция появится здесь."
                    IsOpen="{Binding IsEmpty}" />

        <ListBox Grid.Row="2" ItemsSource="{Binding Items}"
                 BorderThickness="0" Background="Transparent"
                 ScrollViewer.CanContentScroll="True"
                 VirtualizingPanel.IsVirtualizing="True"
                 VirtualizingPanel.VirtualizationMode="Recycling"
                 VirtualizingPanel.ScrollUnit="Pixel"
                 HorizontalContentAlignment="Stretch"
                 Visibility="{Binding IsEmpty, Converter={x:Static conv:InverseBooleanToVisibilityConverter.Instance}}">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0" />
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <ContentPresenter />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:OperationLogRowViewModel}">
                    <Grid MinHeight="64">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="24" />
                            <ColumnDefinition Width="12" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!-- Таймлайн: спина + узел, цвет по исходу -->
                        <Grid Grid.Column="0">
                            <Border Width="2" HorizontalAlignment="Center" VerticalAlignment="Stretch"
                                    Background="{DynamicResource ControlStrokeColorDefaultBrush}" />
                            <Ellipse Width="10" Height="10" VerticalAlignment="Top" Margin="0,20,0,0">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Setter Property="Fill" Value="{DynamicResource TextFillColorTertiaryBrush}" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                <Setter Property="Fill" Value="{DynamicResource SystemFillColorSuccessBrush}" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsFailed}" Value="True">
                                                <Setter Property="Fill" Value="{StaticResource ColorAccentGoldBrush}" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                        </Grid>

                        <Border Grid.Column="2" Style="{StaticResource Cards.RowBorder}" Cursor="Arrow">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding FileName}" FontWeight="SemiBold" FontSize="14"
                                               TextTrimming="CharacterEllipsis" />
                                    <TextBlock FontSize="12" TextTrimming="CharacterEllipsis"
                                               Foreground="{DynamicResource TextFillColorSecondaryBrush}">
                                        <Run Text="{Binding WhenText, Mode=OneWay}" />
                                        <Run Text="·" />
                                        <Run Text="{Binding StatusText, Mode=OneWay}" />
                                    </TextBlock>
                                    <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                        <TextBlock Text="{Binding FromText}" FontSize="11" TextTrimming="CharacterEllipsis"
                                                   MaxWidth="220" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                                        <ui:SymbolIcon Symbol="ArrowRight24" FontSize="12" Margin="6,0"
                                                       Visibility="{Binding IsFailed, Converter={x:Static conv:InverseBooleanToVisibilityConverter.Instance}}"
                                                       Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                                        <TextBlock Text="{Binding ToText}" FontSize="11" TextTrimming="CharacterEllipsis"
                                                   MaxWidth="220" Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                                   Visibility="{Binding IsFailed, Converter={x:Static conv:InverseBooleanToVisibilityConverter.Instance}}" />
                                    </StackPanel>
                                    <ui:HyperlinkButton Content="Открыть в «Неразобранном»" Padding="0" Margin="0,4,0,0"
                                                        Command="{Binding OpenUnsortedCommand}"
                                                        Visibility="{Binding IsFailed, Converter={x:Static conv:BooleanToVisibilityConverter.Instance}}" />
                                </StackPanel>

                                <ui:Button Grid.Column="1" VerticalAlignment="Center" Appearance="Secondary"
                                           Icon="{ui:SymbolIcon ArrowUndo24}" Content="Отменить"
                                           IsEnabled="{Binding CanUndo}"
                                           Command="{Binding DataContext.UndoCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                           CommandParameter="{Binding}">
                                    <ui:Button.Style>
                                        <Style TargetType="ui:Button" BasedOn="{StaticResource RowAction.HoverReveal}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding CanUndo}" Value="False">
                                                    <Setter Property="Opacity" Value="0" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ui:Button.Style>
                                </ui:Button>
                            </Grid>
                        </Border>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>

</UserControl>
