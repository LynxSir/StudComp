using System.Windows.Controls;

namespace StudComp.Views.Archivist;

/// <summary>Вкладка «Неразобранное». Логики нет — всё во ViewModel.</summary>
public partial class UnsortedFilesPage : UserControl
{
    public UnsortedFilesPage() => InitializeComponent();
}
