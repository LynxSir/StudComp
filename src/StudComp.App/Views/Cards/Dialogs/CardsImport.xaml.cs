using System.Windows.Controls;

namespace StudComp.Views.Cards.Dialogs;

/// <summary>Диалог импорта картотеки. Только разметка — вся логика во вьюмодели.</summary>
public partial class CardsImport : UserControl
{
    public CardsImport() => InitializeComponent();
}
