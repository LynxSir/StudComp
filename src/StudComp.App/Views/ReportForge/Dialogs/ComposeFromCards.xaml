<UserControl x:Class="StudComp.Views.ReportForge.Dialogs.ComposeFromCards"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:conv="clr-namespace:StudComp.Resources.Converters"
             xmlns:vm="clr-namespace:StudComp.ViewModels.ReportForge"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:ComposeFromCardsViewModel}"
             Width="480">

    <StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Предмет" FontSize="11" Margin="0,0,0,2"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                <ComboBox ItemsSource="{Binding Subjects}" DisplayMemberPath="Name"
                          SelectedItem="{Binding SelectedSubject, Mode=TwoWay}" HorizontalAlignment="Stretch" />
            </StackPanel>

            <StackPanel Grid.Column="2">
                <TextBlock Text="Колода (необязательно)" FontSize="11" Margin="0,0,0,2"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                <ComboBox ItemsSource="{Binding Decks}" DisplayMemberPath="Name"
                          SelectedItem="{Binding SelectedDeck, Mode=TwoWay}" HorizontalAlignment="Stretch" />
            </StackPanel>
        </Grid>

        <TextBlock Text="Макет" FontSize="11" Margin="0,10,0,2"
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
        <ComboBox ItemsSource="{Binding Layouts}" DisplayMemberPath="Display"
                  SelectedItem="{Binding SelectedLayout, Mode=TwoWay}" HorizontalAlignment="Stretch" />

        <TextBlock Text="Отметьте карточки, которые войдут в отчёт."
                   FontSize="11" TextWrapping="Wrap" Margin="0,10,0,6"
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

        <ui:ProgressRing IsIndeterminate="True" Width="20" Height="20" HorizontalAlignment="Left" Margin="0,4,0,4"
                         Visibility="{Binding IsBusy, Converter={x:Static conv:BooleanToVisibilityConverter.Instance}}" />

        <TextBlock Text="У этого предмета (или колоды) пока нет карточек." FontSize="12" Margin="0,4,0,0"
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                   Visibility="{Binding HasCards, Converter={x:Static conv:InverseBooleanToVisibilityConverter.Instance}}" />

        <ListBox ItemsSource="{Binding Cards}" MaxHeight="320" BorderThickness="0" Background="Transparent"
                 ScrollViewer.CanContentScroll="True"
                 VirtualizingPanel.IsVirtualizing="True"
                 VirtualizingPanel.VirtualizationMode="Recycling"
                 VirtualizingPanel.ScrollUnit="Pixel"
                 HorizontalContentAlignment="Stretch"
                 Visibility="{Binding HasCards, Converter={x:Static conv:BooleanToVisibilityConverter.Instance}}">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0" />
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <ContentPresenter />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:CardSelectionRowViewModel}">
                    <Grid Margin="0,3">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="8" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <CheckBox Grid.Column="0" VerticalAlignment="Top" Margin="0,2,0,0"
                                  IsChecked="{Binding IsSelected, Mode=TwoWay}" />

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="{Binding Front}" FontWeight="SemiBold" FontSize="13"
                                       TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{Binding Back}" FontSize="11" TextTrimming="CharacterEllipsis"
                                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </StackPanel>

</UserControl>
