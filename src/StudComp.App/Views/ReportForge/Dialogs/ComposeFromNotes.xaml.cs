using System.Windows.Controls;

namespace StudComp.Views.ReportForge.Dialogs;

/// <summary>Диалог «Собрать из заметок». Логики нет — всё во ViewModel.</summary>
public partial class ComposeFromNotes : UserControl
{
    public ComposeFromNotes() => InitializeComponent();
}
