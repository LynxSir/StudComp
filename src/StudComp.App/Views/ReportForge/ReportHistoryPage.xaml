<UserControl x:Class="StudComp.Views.ReportForge.ReportHistoryPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:conv="clr-namespace:StudComp.Resources.Converters"
             xmlns:vm="clr-namespace:StudComp.ViewModels.ReportForge"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:ReportHistoryViewModel}"
             d:DesignHeight="600" d:DesignWidth="900">

    <Grid Margin="0,12,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock Text="Сгенерированные отчёты" FontWeight="SemiBold" />
                <TextBlock Text="Последние запуски — с ссылкой на файл на диске." FontSize="11"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            </StackPanel>

            <ui:Button Grid.Column="1" Appearance="Secondary" Command="{Binding RefreshCommand}"
                       Icon="{ui:SymbolIcon ArrowSync24}" Content="Обновить" />
        </Grid>

        <!-- Фильтр по предмету -->
        <Grid Grid.Row="1" Margin="0,0,0,12"
              Visibility="{Binding IsEmpty, Converter={x:Static conv:InverseBooleanToVisibilityConverter.Instance}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ui:SymbolIcon Grid.Column="0" Symbol="Filter24" VerticalAlignment="Center" Margin="0,0,8,0"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            <ComboBox Grid.Column="1" MinWidth="180"
                      ItemsSource="{Binding SubjectFilterChoices}" DisplayMemberPath="Name"
                      SelectedItem="{Binding SelectedSubjectFilter, Mode=TwoWay}" />

            <ui:Button Grid.Column="2" HorizontalAlignment="Right" Appearance="Transparent"
                       Content="Сбросить фильтр" Command="{Binding ResetFilterCommand}"
                       Visibility="{Binding HasActiveFilter, Converter={x:Static conv:BooleanToVisibilityConverter.Instance}}" />
        </Grid>

        <StackPanel Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="420"
                    Visibility="{Binding IsEmpty, Converter={x:Static conv:BooleanToVisibilityConverter.Instance}}">
            <ui:SymbolIcon Symbol="DocumentText24" FontSize="42" HorizontalAlignment="Center"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            <ui:TextBlock Text="Отчётов пока нет" FontTypography="Subtitle" TextAlignment="Center"
                          Margin="0,12,0,4" />
            <TextBlock Text="Соберите первый на вкладке «Новый отчёт» — он появится здесь."
                       TextWrapping="Wrap" TextAlignment="Center"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
        </StackPanel>

        <ListBox Grid.Row="2" ItemsSource="{Binding Items}"
                 BorderThickness="0" Background="Transparent"
                 ScrollViewer.CanContentScroll="True"
                 VirtualizingPanel.IsVirtualizing="True"
                 VirtualizingPanel.VirtualizationMode="Recycling"
                 VirtualizingPanel.ScrollUnit="Pixel"
                 HorizontalContentAlignment="Stretch"
                 Visibility="{Binding IsEmpty, Converter={x:Static conv:InverseBooleanToVisibilityConverter.Instance}}">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0" />
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <ContentPresenter />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:ReportJobRowViewModel}">
                    <Border Margin="0,0,0,8" CornerRadius="10" Padding="14,10" BorderThickness="1"
                            Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="8" ShadowDepth="1" Direction="270" Opacity="0.10" Color="Black" />
                        </Border.Effect>
                        <Border.Style>
                            <Style TargetType="Border">
                                <Style.Triggers>
                                    <!-- Готовый отчёт — золотая рамка как знак завершённости (ARCHITECTURE §19.2). -->
                                    <DataTrigger Binding="{Binding IsReady}" Value="True">
                                        <Setter Property="BorderBrush" Value="{StaticResource ColorAccentGoldBrush}" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsFailed}" Value="True">
                                        <Setter Property="BorderBrush" Value="{DynamicResource SystemFillColorCriticalBrush}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="12" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <ui:SymbolIcon Grid.Column="0" Symbol="DocumentText24" VerticalAlignment="Center"
                                           Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

                            <StackPanel Grid.Column="2">
                                <TextBlock Text="{Binding FileName}" FontWeight="SemiBold" FontSize="14"
                                           TextTrimming="CharacterEllipsis" />
                                <TextBlock Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="12"
                                           TextTrimming="CharacterEllipsis">
                                    <Run Text="{Binding SubjectName, Mode=OneWay}" />
                                    <Run Text="·" />
                                    <Run Text="{Binding StatusText, Mode=OneWay}" />
                                    <Run Text="·" />
                                    <Run Text="{Binding CreatedText, Mode=OneWay}" />
                                </TextBlock>
                                <TextBlock Text="{Binding FolderName}" FontSize="11" TextTrimming="CharacterEllipsis"
                                           Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                                           Visibility="{Binding FileExists, Converter={x:Static conv:BooleanToVisibilityConverter.Instance}}" />
                                <TextBlock Text="Файл перемещён или удалён" FontSize="11"
                                           Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                                           Visibility="{Binding IsMissing, Converter={x:Static conv:BooleanToVisibilityConverter.Instance}}" />
                            </StackPanel>

                            <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center">
                                <StackPanel Orientation="Horizontal"
                                            Visibility="{Binding FileExists, Converter={x:Static conv:BooleanToVisibilityConverter.Instance}}">
                                    <ui:Button Appearance="Primary" Padding="10,4" Margin="0,0,4,0"
                                               Command="{Binding DataContext.OpenCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                               CommandParameter="{Binding}"
                                               Content="Открыть" />
                                    <ui:Button Appearance="Secondary" Padding="8,4" Margin="0,0,4,0" ToolTip="Показать в проводнике"
                                               Command="{Binding DataContext.RevealCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                               CommandParameter="{Binding}"
                                               Icon="{ui:SymbolIcon FolderOpen24}" />
                                </StackPanel>

                                <!-- Hover-reveal (конвенция Phase 12.2, OperationsLogPage) -->
                                <StackPanel Orientation="Horizontal">
                                    <StackPanel.Style>
                                        <Style TargetType="StackPanel">
                                            <Setter Property="Opacity" Value="0" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True">
                                                    <Setter Property="Opacity" Value="1" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </StackPanel.Style>
                                    <ui:Button Appearance="Secondary" Padding="8,4" Margin="0,0,4,0" ToolTip="Сгенерировать заново"
                                               Command="{Binding DataContext.RegenerateCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                               CommandParameter="{Binding}"
                                               Icon="{ui:SymbolIcon ArrowSync24}" />
                                    <ui:Button Appearance="Secondary" Padding="8,4" ToolTip="Удалить запись"
                                               Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                               CommandParameter="{Binding}"
                                               Icon="{ui:SymbolIcon Delete24}" />
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>

</UserControl>
