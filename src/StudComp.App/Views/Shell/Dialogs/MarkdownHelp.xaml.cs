using System.Windows.Controls;

namespace StudComp.Views.Shell.Dialogs;

/// <summary>Шпаргалка по Markdown. Логики нет — содержимое статично и живёт во ViewModel.</summary>
public partial class MarkdownHelp : UserControl
{
    public MarkdownHelp() => InitializeComponent();
}
