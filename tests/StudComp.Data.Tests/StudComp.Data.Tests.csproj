<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="coverlet.collector" Version="6.0.4" />
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.14.1" />
    <PackageReference Include="xunit" Version="2.9.3" />
    <PackageReference Include="xunit.runner.visualstudio" Version="3.1.4" />
    <!-- Явно, для читаемости — транзитивно уже приходит через StudComp.Data. -->
    <PackageReference Include="Microsoft.EntityFrameworkCore.Sqlite" Version="10.0.9" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Xunit" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\StudComp.Data\StudComp.Data.csproj" />
    <!-- Тестовая связь Phase 13.8: сквозной round-trip бэкапа нужен настоящий BackupService/
         SqliteDatabaseBackupPort вместе с реальным StudCompDbContext — production-код Data
         по-прежнему не ссылается на Infrastructure, это исключительно ProjectReference тестового
         проекта. -->
    <ProjectReference Include="..\..\src\StudComp.Infrastructure\StudComp.Infrastructure.csproj" />
  </ItemGroup>

</Project>
